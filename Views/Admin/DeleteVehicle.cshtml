@model VehicleManagement.Models.Vehicle

@{
    ViewData["Title"] = $"Admin - Delete Vehicle: {Model.Make} {Model.Model}";
}

<div class="container py-5">
    <div class="text-center mb-5">
        <h2 class="display-4 fw-bold text-white text-shadow-sm">
            <i class="bi bi-trash-fill me-3"></i> Admin: Delete Vehicle
        </h2>
        <p class="lead text-light opacity-75">
            Are you sure you want to delete this vehicle record? This action cannot be undone.
        </p>
    </div>

    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-7">
            <div class="card shadow-lg border-0 rounded-4 p-4 animate-fade-in">
                <div class="card-body">
                    <h4 class="card-title text-center mb-4 text-dark">Vehicle Details</h4>
                    <dl class="row detail-list">
                        <dt class="col-sm-4 text-dark">Registration Number</dt>
                        <dd class="col-sm-8 text-muted">@Model.RegistrationNumber</dd>

                        <dt class="col-sm-4 text-dark">Make</dt>
                        <dd class="col-sm-8 text-muted">@Model.Make</dd>

                        <dt class="col-sm-4 text-dark">Model</dt>
                        <dd class="col-sm-8 text-muted">@Model.Model</dd>

                        <dt class="col-sm-4 text-dark">Type</dt>
                        <dd class="col-sm-8 text-muted">@Model.Type</dd>

                        <dt class="col-sm-4 text-dark">Status</dt>
                        <dd class="col-sm-8">
                            <span class="badge rounded-pill px-3 py-2 @(Model.Status == "Active" ? "bg-success" : (Model.Status == "Maintenance" ? "bg-warning text-dark" : "bg-danger"))">
                                @Model.Status
                            </span>
                        </dd>

                        <dt class="col-sm-4 text-dark">Last Maintenance Date</dt>
                        <dd class="col-sm-8 text-muted">@(Model.LastMaintenanceDate?.ToShortDateString() ?? "N/A")</dd>

                        <dt class="col-sm-4 text-dark">Assigned User</dt>
                        <dd class="col-sm-8 text-muted">@(Model.User?.UserName ?? "N/A")</dd>
                    </dl>

                    <hr class="my-4" />

                    <!-- CORRECTED: asp-action is now "DeleteVehicle" to match ActionName in controller -->
                    <form asp-controller="Admin" asp-action="DeleteVehicle" method="post">
                        <input type="hidden" asp-for="Id" />
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-custom btn-danger-custom">
                                <i class="bi bi-trash me-2"></i> Confirm Delete
                            </button>
                            <a asp-controller="Admin" asp-action="Index" class="btn btn-custom btn-outline-secondary-custom mt-3">
                                <i class="bi bi-x-circle-fill me-2"></i> Cancel
                            </a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <!-- Bootstrap Icons (if not already included in _Layout) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
}

@section Styles {
    <style>
        /* Reusing global styles */
        .text-shadow-sm {
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }

        .card {
            background-color: rgba(255, 255, 255, 0.95);
        }

        .rounded-4 {
            border-radius: .75rem !important;
        }

        .btn-custom { /* ... styles ... */
        }

        .btn-danger-custom { /* ... styles ... */
        }

        .btn-outline-secondary-custom { /* ... styles ... */
        }

        .detail-list dt { /* ... styles ... */
        }

        .detail-list dd { /* ... styles ... */
        }

        .badge.bg-success { /* ... styles ... */
        }

        .badge.bg-warning { /* ... styles ... */
        }

        .badge.bg-danger { /* ... styles ... */
        }

        .animate-fade-in {
            animation: fadeIn 0.8s ease-out;
        }

        @@keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
}
